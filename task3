# 3. Реализовать функцию my_func(), которая принимает три позиционных аргумента,
# и возвращает сумму наибольших двух аргументов.

def my_func(num_1,num_2, num_3):
    """Функция возвращает сумму двух самых больших аргументов
    Ввод - вещественные или целые числа
    Вывод результат"""
    return num_1 + num_2 + num_3 - min(num_1,num_2, num_3)

while True:  # Ввод оформил отдельным блоком с проверкой и переводом в вещественные
    num_1, num_2, num_3 = input('Введите 1-е число '), input('Введите 2-е число '), input('Введите 3-е число ')
    try:
        num_1 = float(num_1)
        num_2 = float(num_2)
        num_3 = float(num_3)
        break
    except ValueError:
        print('А ведь я всего лишь просил ввести числа. Как несовершенен это мир...')

print(round(my_func(num_1,num_2, num_3),2))
