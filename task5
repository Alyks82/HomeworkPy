# 5. Создать (программно) текстовый файл, записать в него программно набор чисел, разделенных пробелами.
# Программа должна подсчитывать сумму чисел в файле и выводить ее на экран.

from random import randint

with open('num_array.txt', 'w') as newborn:
    '''генератор на случайное количество чисел до 100 со значением 1-1000'''
    num_list = [randint(1, randint(1, 1000)) for _ in range(1, randint(1, 100))]
    newborn.write(' '.join(map(str, num_list)))

with open('num_array.txt', 'r') as food4count:
    content = food4count.read().split(' ')  # обратное действие
    print(f'Содержание записанного файла: {content}')
    print(f'Итоговая сумма всех элементов: {sum([int(n) for n in content])}')
