# 5. Программа запрашивает у пользователя строку чисел,
# разделенных пробелом. При нажатии Enter должна выводиться
# сумма чисел. Пользователь может продолжить ввод чисел,
# разделенных пробелом и снова нажать Enter. Сумма вновь
# введенных чисел будет добавляться к уже подсчитанной сумме.
# Но если вместо числа вводится специальный символ, выполнение
# программы завершается. Если специальный символ введен после
# нескольких чисел, то вначале нужно добавить сумму этих чисел
# к полученной ранее сумме и после этого завершить программу.

def filter(input_line):
    '''Из вводимой строки создаём список из чисел'''
    new_list = input_line.split()
    for item in new_list[::]:
        if item.isdigit():
            continue
        else:
            new_list.remove(item)
    return new_list

total_sum = 0
while True:
    input_line = input('Введите сроку чисел разделённых пробелом. При введении Q произойдёт выход из программы ')
    local_sum = 0
    for el in filter(input_line):
        total_sum += int(el)
        local_sum += int(el)
    print(f'Общая сумма введённых цифр: {total_sum}\nВ этот раз Вы ввели цифр на сумму: {local_sum}')
    if input_line.count("Q") > 0 or input_line.count("q") > 0:
        break


print("Спасибо за использование нашей программы")
