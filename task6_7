# 6. Реализовать функцию int_func(), принимающую слово из
# маленьких латинских букв и возвращающую его же, но с прописной
# первой буквой. Например, print(int_func(‘text’)) -> Text.
# Продолжить работу над заданием. В программу должна попадать
# строка из слов, разделенных пробелом. Каждое слово состоит
# из латинских букв в нижнем регистре. Сделать вывод исходной
# строки, но каждое слово должно начинаться с заглавной буквы.
# Необходимо использовать написанную ранее функцию int_func().

def int_func(line):
    '''Функция для трансформации слов
    Ввод слова написанные прописнымы латинскими буквами,
    Вывод слова с заглавными буквами'''
    return line.title()

# В этом задании я немного не понял, можно ли использовать .title(),
# но в принципе можно легко разбить на список и пройтись .capitalize()
# Если и так нельзя, то тогда список, перебор и словарь соответствия???

while True:
    err = False
    input_line = input('Введите слово/строку из прописных латинских букв разделённых пробелами. '
                       'Введите exit, чтобы выйти ')
    if input_line.count("exit") > 0 or input_line.count("EXIT") > 0: # выход до основной проверки
        break
    for letter in input_line: # проверяем точно ли маленькая латинская или пробел
        if (ord(letter) > 121 or ord(letter) < 97) and ord(letter)!= 32:
            err = True          # согласен, что очень громоздко, но лучше на ум не пришло :(
            print(f"Обнаружен символ, не отвечающий условию: {letter}")
            break
        else:
            continue
    if err:
        print('С прискорбием сообщаю, что условие не выполнено, попробуйте ещё раз')
    else:
        print(int_func(input_line))
