# 1. Реализовать функцию, принимающую два числа (позиционные аргументы) и выполняющую их деление.
# Числа запрашивать у пользователя, предусмотреть обработку ситуации деления на ноль.

def dev_func(num_1, num_2):
    """ Функция, производящая деление двух позиционных аргументов
    Результат: округлённое до 4 цифры частое или уведомление о ошибке
    """
    try:  # отработка исключения на цифры
        num_1 = float(num_1)   # решил переводить в вещественные числа для большего функционала
        num_2 = float(num_2)
        try:
            return round(num_1 / num_2, 4)  # округление до 4 знака, для красоты :)
        except ZeroDivisionError:    # отработка исключения деления на ноль
            return "Как поделить на ноль, пока не придумали"
    except ValueError:
        return 'Заниматься арифметикой без реальных цифр - удел философии ;)'


print(dev_func(input('Введите первое число '), input('Введите второе число ')))
