# 6. Необходимо создать (не программно) текстовый файл, где каждая строка описывает учебный предмет
# и наличие лекционных, практических и лабораторных занятий по этому предмету и их количество.
# Важно, чтобы для каждого предмета не обязательно были все типы занятий. Сформировать словарь,
# содержащий название предмета и общее количество занятий по нему. Вывести словарь на экран.
# Примеры строк файла:
# Информатика: 100(л) 50(пр) 20(лаб).
# Физика: 30(л) — 10(лаб)
# Физкультура: — 30(пр) —
# Пример словаря:
# {“Информатика”: 170, “Физика”: 40, “Физкультура”: 30}

full_dict = {}
with open('text_6.txt', 'r', encoding='UTF-8') as tr:
    '''Просто режу построчно, нахожу по срезу ключ, а в словах выношу числа'''
    for line in tr.readlines():
        r = line.split(' ')
        sum = 0
        for el in r:
            # числа ищу через определение его номера кодировки 48-57
            num = [letter for letter in el if ord(letter) > 47 and ord(letter) < 58]
            if num != []:
                sum += int(''.join(num))
        full_dict[r[0][:-1]] = sum

print(full_dict)
